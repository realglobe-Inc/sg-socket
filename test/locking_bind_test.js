/**
 * Test case for lockingBind.
 * Runs with mocha.
 */
'use strict'

const lockingBind = require('../lib/bindings/locking_bind.js')
const assert = require('assert')
const co = require('co')

describe('locking-bind', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Locking bind', () => co(function * () {

  }))
})

/* global describe, before, after, it */
